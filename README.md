# HTML DIFF tool

## Setup

1. Installing npm modules

```
npm install
```

2. Write configuration

To get HTML source and compare, write your host and url to ``download.js``.

```
e.g.

const config = {
  production: 'www.production.com',
  staging: 'www.staging.com',
  url: '/path/to/content'
};
```
